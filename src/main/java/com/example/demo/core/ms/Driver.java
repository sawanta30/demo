package com.example.demo.core.ms;

import java.util.Arrays;
import java.util.Set;
import java.util.TreeSet;
import java.util.concurrent.BlockingDeque;
import java.util.concurrent.LinkedBlockingDeque;
import java.util.concurrent.TimeUnit;

public class Driver {
    public static void main(String[] args) throws InterruptedException {
        C c = new C();
//        Object[] myObj = {new Integer(5),new String("foo"),new Integer(5),new Boolean(true)};
//        Arrays.sort(myObj);
//
        Car<String> cs = new Car<>();
        Car cws = cs;

//        BlockingDeque<Integer> dq = new LinkedBlockingDeque<>();
//        dq.offerLast(10,5, TimeUnit.SECONDS);
//        System.out.println(dq.pollLast(5,TimeUnit.SECONDS));
//        System.out.println(dq.pollFirst(5,TimeUnit.SECONDS));
//
//        Set set = new TreeSet();
//        set.add(null);
//        System.out.println(set);

    }
}
